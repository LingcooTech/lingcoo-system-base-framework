# @lingcootech/frame-jobs

Optional PostgreSQL-backed Jobs and transactional Outbox feature. It owns its
REST administration routes, worker registries, persistence services and
migration source. Audit integration is injected through `JobsPorts`.
